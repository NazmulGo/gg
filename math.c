#include<stdio.h>
int main()
{
    int n, i, j, gg=0;
    scanf("%d", &n);
    if(n%2==0)
    {
        for(i=2; i<n; i++)
        {
            if(n%i == 0)
            {
                printf("%d %d", )
            }
        }
    }
}
